package main

import "fmt"

// Eine zweidimensionale Liste (Matrix) enthält Zahlen.
// Eine Zahl ist dann "magisch", wenn sie in ihrer Zeile und ihrer Spalte die größte Zahl ist.
//
// Schreibe ein Programm, das alle "magischen" Elemente in der Matrix findet.
// Nutze verschachtelte for-Schleifen.

// Zusatz: Gib für jedes gefundene magische Element den Wert und die Position aus.

// bevor du das programm ausführst, musst du geschriebene änderungen mit strg + s speichern
// Zum ausführen vom code musst du oben new terminal auswählen
// cd dein ordner (in aktuelles verzeichnis wechseln)
// go run main.go

// in diese Funktion (func) muss der gesamte code geschrieben werden, damit er ausgeführt wird
func main() {
	fmt.Println()

	// TODO: Matrix erstellen
	// Beispiel:
	// matrix := [][]int{
	//     {3, 8, 4},
	//     {1, 9, 2},
	//     {5, 7, 6},
	// }

	// TODO: Durch jede Zelle iterieren (i = Zeile, j = Spalte)
	// TODO: Prüfen, ob die Zahl die größte in ihrer Zeile UND Spalte ist
	// TODO: Wenn ja, ausgeben: Wert und Position
}
