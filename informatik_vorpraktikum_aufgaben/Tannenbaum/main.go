package main

import "fmt"

// schreibe ein programm, das bei einer gegebenen höhe einen tannenbaum aus sternchen (*) ausgibt.
// zum beispiel soll bei höhe 4 folgender baum entstehen:
//    *
//   ***
//  *****
// *******
//
// definiere eine variable `höhe` mit einem ganzzahligen wert (z. B. 4)
// nutze zwei verschachtelte for-schleifen: eine für die zeilen und eine für die zeichen in jeder zeile
//
// bevor du das programm ausführst, musst du geschriebene änderungen mit strg + s speichern
// zum ausführen vom code musst du oben "new terminal" auswählen
// cd dein ordner (in aktuelles verzeichnis wechseln)
// go run main.go

// in diese funktion (func) muss der gesamte code geschrieben werden, damit er ausgeführt wird
func main() {
	fmt.Println()
	// TODO
}
